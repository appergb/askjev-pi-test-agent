# 改动记录

## 未发布

- 增加 `askjev-cli` 交互终端：askJEV 品牌、中文对话、测试进度、结果与证据路径，复用既有测试和原测试回归流程。
- 交互入口固定使用私密配置默认模型，移除模型选择入口，不接受模型切换参数或任务级覆盖。
- 支持取消、退出时清理对话运行时并保留测试证据；技能安装器携带交互命令。
- 修复代理请求取消时遗留并发名额、分块请求超限前仍继续读取的问题；流结束和清理异常均归还名额。
- 修复宿主覆盖率采集导致隔离测试误判失败；增加 GitHub Actions 与公开质量评估。

## 1.5.0

- 新增 `campaign` 与后台 `session campaign`：固定源码快照、多轮独立对话、未引用需求/已测场景反馈、失败原测试重现、汇总交接与持续落盘。
- 总时间、轮数、模型轮次与停滞条件限制任务；原始源码变化会停止；取消、失败和未复现不会报告为完整通过。
- `score-quality.json` 显式记录未知评分、六位小数并列和极端饱和；`--scoring-failure all` 允许全量测试在评分服务失败时继续，保留 null 评分。
- 重打包独立编码技能；新增持续任务说明及实际云端验收摘要。

## 1.4.0 — 独立技能安装包与持久会话

- 增加独立 askjev-agent 编码 Skill、固定版本运行时打包、SHA-256 校验、自安装与匹配版本调用脚本。
- 安装配套 askjev / askJEV / pi-test-agent 入口、内置运行库与批准的 Skills，不分发用户凭据。
- 增加 create/list/run/inspect/logs/stop/clear/restart 会话管理、后台任务、独立持久上下文和每会话运行锁。
- 支持多个会话并行；每轮仍需新快照、重新评分和实际测试证据；clear 保留历史测试产物。
- 明确这是应用级生命周期，尚非 Docker 隔离，也不具备 CPU/内存配额与执行断点恢复。


## 1.3.0 — 前端浏览器测试与按分数选测

- 增加 Chrome 静态快照执行器、声明式 browser-tests Skill、步骤断言和截图证据。
- 增加 all、lowest、highest、range 选测策略，程序强制执行选中集合，未测项单独列出。
- 增加 replay，在相同源码、分数和测试计划上比较不同选测策略；修复回归执行不变测试。
- 新增可交互的购物结算样例与独立修复版，植入两个已知缺陷用于受控实验。
- 实测低分三项检出一个问题，全量六项检出两个；另一个缺陷被评分为 1.0，未证明只测低分足够。


## 1.2.0 — 独立 CLI 与云端连接

- 产品统一为 askJEV Agent，定位为优化过的 Agent 框架；保留上游来源和历史兼容标识。
- 增加可安装的 `askjev`、帮助/版本、配置初始化与导入、独立 demo、模型工具调用探针和分项健康检查。
- 整合 DGX Spark SSH 连接的启动、状态与关闭，连接信息不进入命令输出。
- 配置和运行产物移出安装目录；修复 npm bin 符号链接启动，以及交接包回归命令依赖当前目录的问题。
- 使用安装包文件白名单与 npm-shrinkwrap 固定依赖，增加独立应用测试与排查能力路线。
- 验证：31 项基础设施检查通过；安装版真实 demo 8 项检查检出两个预置缺陷，修复后 8/8 通过；klona 原测试回归 8/8，上游基线 49/49。
- 测试执行仍限本地 macOS，新增连接封装不代表支持 Spark 上运行测试。


## 1.1.0 · 2026-09-21

从本地 demo 扩展到真实小型 JavaScript 仓库的测试与修复反馈闭环。

### 测试运行时

- 支持直接读取 `.js` / `.mjs` / `.cjs`，无需改名上游源码。
- 增加 node:test / uvu 原测试基线；基线失败时阻断新缺陷发现。
- 执行文件与模型上下文分离，原测试参与快照摘要但不全部发送给模型。
- 增加 Git 提交与工作区修改状态，保留未提交内容的快照身份。
- 以需求引用 ID 解析原文，加入最少检查项预算，避免反复校验后范围缩水。
- 评分只接收引用源码和需求摘录，修复整份 README 导致后端上下文超限的问题。

### coding agent 集成

- 增加 `handoff`、`regress`、`feedback` CLI 流程。
- 交接文件包含缺陷、预期来源、测试摘要、执行证据和回归参数。
- 回归沿用原 Pi 测试，校验关联和内容摘要，记录逐项结果。
- 由执行器附上真实日志，避免把模型生成的摘录直接当证据。
- 严重性保持待评估，确认失败记录与独立 bug 数分别解释。

### 验证

- 固定 klona 2.0.6 的上游提交，发现 DataView 偏移/长度丢失这一类缺陷，影响两个实现位置。
- 修复后的 16 组原 Pi 测试与 137 个上游测试通过。
- 基础设施测试扩展到 25 项，涵盖协议、基线、隔离、取消、预算和反馈关联。
- 保留未通过结论：真实失败项仍被评分模型给出最高“满足”分；不依据高分跳过测试。

### 仓库整理

- 建立独立私有仓库 `appergb/askjev-pi-test-agent`；保留原环境文档仓库关联。
- 重写 README，补充首次安装、配置样例、命令、目录说明和完成边界。
- 增加可提交的验收摘要；本机日志、凭据、模型配置和克隆产物继续由 Git 排除。
- 更新文档索引与协作规则，将部署脚本的固定用户目录改为可配置运行目录。

## 1.0.0 · 2026-09-21

- 基于固定 Pi 0.85.1 SDK 建立专用测试运行时和 `askJEV` 适配器。
- 交付四个 Pi Skills 与一个 Codex Skill，以及任务快照、沙箱执行和报告。
- 本地任务存储 demo 完成真实云端评分、缺陷复现和修复回归。
- 验证 Flash、GLM 5.3 和云端千问；默认采用已测速度较快的 Flash 直连。
- 初版基础设施检查 19 项通过；明确评分排序尚不可靠。
